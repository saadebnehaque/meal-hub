// import React from 'react';

const Spinner = () => {
    return (
        <div className="spinnerContainer">
            <div className="spinner">
            </div>
            <h2>Food is loading....</h2>
        </div>

    );
};

export default Spinner;